#include <stdio.h>

int main() {
  int a, n, i, r=0;
  
  scanf("%d %d", &a, &n);
  while(n<=0) {
    scanf("%d", &n);  
  }
    
  for(i=1; i<=n; i++) {
    r+=a;
    a++;
  }

  printf("%d\n",r);
  return 0;
}

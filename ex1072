#include <stdio.h>

int main(void) {
  int n, x, i=0, o=0;

  scanf("%d",&n);
  while(n>0) {
    scanf("%d",&x);
    if(x>=10 && x<=20){
      i++;
    } else {
      o++;
    }
    n--;
  }
  printf("%d in\n%d out\n", i, o);
  
  return 0;
}

#include <stdio.h>

int main(void) {
  int n, i, maior=0, indice;
  for(i=1;i<=100;i++){
    scanf("%d",&n);
    if (n>maior) {
      maior=n;
      indice=i;
    }
  }
  printf("%d\n%d\n", maior, indice);
  return 0;
}

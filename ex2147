#include <stdio.h>
#include <string.h>

int main() {
  int c;
  char palavra[10001];
  scanf("%d", &c);
  while(c>0) {
    scanf("%s", palavra);
    printf("%.2lf\n", strlen(palavra)*.01);
    c--;
  }
  return 0;
}

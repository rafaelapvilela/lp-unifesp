#include <stdio.h>
#include <stdlib.h>

int main()
{
    int idade, a, m, d;
    scanf("%d", &idade);
    a = idade/365;
    m = (idade%365)/30;
    d = idade - ((a*365) + (m*30));
    printf("%d ano(s)\n%d mes(es)\n%d dia(s)\n", a, m, d);
    return 0;
}

#include <stdio.h>

int main() {
  double n, resp = 0.0;

  scanf("%lf", &n);
  while(n>0) {
    resp += 6.0;
    resp = 1.0/resp;
    n--;
  }

  resp += 3.0;
  printf("%.10lf\n", resp);

  return 0;
}

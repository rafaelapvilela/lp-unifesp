#include <stdio.h>

int main(void) {
  int i;
  double s=0.0, p=0.0, x;
  for (i=0; i<6; i++) {
    scanf("%lf", &x);
    if (x>0) {
      p = p + 1.0;
      s = s + x;
    }
  }

  printf("%.0lf valores positivos\n%.1lf\n", p, s/p);
  return 0;
}

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int tempo, h, m, s;
    scanf("%d",&tempo);
    h = (tempo/3600);
    m = (tempo%3600)/60;
    s = (tempo%60);
    printf("%d:%d:%d\n", h, m, s);
    return 0;
}
